<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.song.dao.IRegisterDao">
	
	<select id="save">
		insert into t_user(job_number, user_name, gender, email, education, major_id)
		values
		(#{jobNumber},#{userName},#{gender},#{email},#{education}, #{majorId});
	</select>
</mapper>